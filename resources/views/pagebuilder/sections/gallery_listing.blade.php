@pbSchema(['name' => 'gallery_listing.blade'])
@php
    $items=\App\Models\GalleryItem::query()->forLanguage($lang)->active()->orderBy('sort_order')->get();
@endphp
<main class="gallery-page" id="ve-page-root" data-ve-root><div class="container"><div class="gallery-head"><h1 data-inline-field="title">{{ $content['title']??'' }}</h1><p data-inline-field="intro">{{ $content['intro']??'' }}</p></div><div class="gallery-grid" style="--gallery-cols:{{ max(2,min(4,(int)($content['columns']??4))) }}">@forelse($items as $item)<button class="gallery-card" type="button" data-image="{{ asset(ltrim($item->image_path,'/')) }}" data-title="{{ $item->title }}" data-description="{{ $item->description }}"><img src="{{ asset(ltrim($item->image_path,'/')) }}" alt="{{ $item->title }}"><span class="gallery-overlay"><span><strong data-entity="gallery" data-entity-id="{{ $item->id }}" data-entity-field="title">{{ $item->title }}</strong>@if($item->description)<span data-entity="gallery" data-entity-id="{{ $item->id }}" data-entity-field="description">{{ $item->description }}</span>@endif</span></span><span class="gallery-eye"><i class="fa-solid fa-expand"></i></span></button>@empty<div class="gallery-empty">{{ $ui['no_gallery']??'' }}</div>@endforelse</div></div></main>
<div class="gallery-modal" id="galleryModal"><div class="gallery-modal-box"><img class="gallery-modal-img" id="modalImage" alt=""><div class="gallery-modal-info"><div><h3 id="modalTitle"></h3><p id="modalDescription"></p></div><button class="modal-close" id="modalClose" type="button"><i class="fa-solid fa-xmark"></i></button></div></div></div>
