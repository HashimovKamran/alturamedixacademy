@pbSchema(['name' => 'profile_card.blade'])
@if(isset($user))<section class="profile-card"><div class="profile-head"><div class="avatar">{{ mb_substr($user->full_name,0,1,'UTF-8') }}</div><div><h1 data-inline-field="title">{{ \App\Support\Cms\NativeBlockOptions::text($content,'title',$user->full_name) }}</h1>@if(\App\Support\Cms\NativeBlockOptions::enabled($content,'show_email'))<p>{{ $user->email }}</p>@endif</div></div><div class="profile-grid"><div class="profile-field"><span>{{ $ui['phone'] }}</span><strong>{{ $user->phone?:'-' }}</strong></div><div class="profile-field"><span>{{ $ui['status'] }}</span><strong>{{ $user->is_active?$ui['active']:$ui['inactive'] }}</strong></div></div>@if(\App\Support\Cms\NativeBlockOptions::enabled($content,'show_logout'))<a class="btn btn-outline" href="{{ route('site.logout') }}">{{ $settings['auth_logout']??'Çıxış' }}</a>@endif</section>@endif
