@pbSchema(['name' => 'contact_grid.blade'])
<main class="contact-page" id="ve-page-root" data-ve-root><div class="container contact-grid">@foreach($children->filter(fn($child)=>$child['block']->slot_key==='info') as $child)@include('pagebuilder.partials.node',['node'=>$child,'embedded'=>true])@endforeach @foreach($children->filter(fn($child)=>$child['block']->slot_key==='form') as $child)@include('pagebuilder.partials.node',['node'=>$child,'embedded'=>true])@endforeach</div>@foreach($children->filter(fn($child)=>$child['block']->slot_key==='map') as $child)@include('pagebuilder.partials.node',['node'=>$child,'embedded'=>true])@endforeach</main>
