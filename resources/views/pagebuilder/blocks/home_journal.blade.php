@pbSchema(['name' => 'home_journal.blade'])
@php $journal=\App\Models\Block::query()->forLanguage($lang)->active()->where('block_key','journal')->first(); @endphp
@if($journal)<div class="promo-row"><div class="journal-box">@if($journal->image_path)<div class="promo-bg" style="background-image:url('{{ asset(ltrim($journal->image_path,'/')) }}')"></div>@endif<div class="promo-content"><h3 data-entity="block" data-entity-id="{{ $journal->id }}" data-entity-field="title">{{ $journal->title }}</h3><p data-entity="block" data-entity-id="{{ $journal->id }}" data-entity-field="body">{{ $journal->body }}</p>@if($journal->button_text)<a href="{{ \App\Support\CleanUrl::to($journal->button_url?:'#',$lang) }}" class="btn btn-dark-outline"><span data-entity="block" data-entity-id="{{ $journal->id }}" data-entity-field="button_text">{{ $journal->button_text }}</span> <i class="fa-solid fa-arrow-right"></i></a>@endif</div></div></div>@endif
